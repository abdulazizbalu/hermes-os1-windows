# OS1 Windows Shell Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first working Windows OS1 desktop slice: Electron app, React shell, OS1 design system, boot experience, section navigation, typed IPC skeleton, tests, and Windows `.exe` release workflow.

**Architecture:** Create a fresh Electron + React + TypeScript app with a privileged main process, isolated preload bridge, and renderer-only UI. Keep OS1 visual tokens in CSS/TypeScript, route all native actions through typed IPC, and ship a Windows NSIS installer through GitHub Actions.

**Tech Stack:** Electron 39, React 19, TypeScript 5.9, Vite 7, Vitest, Testing Library, Playwright, electron-builder, xterm.js reserved for the next terminal plan.

---

## Scope Check

The approved design spec covers full OS1 parity across Orgo, terminal, sessions, kanban, files, cron, voice, desktop, and providers. That is too large for one implementation plan. This plan implements the foundation slice only. Later plans should add:

- Orgo Connections and credential storage
- Orgo terminal and Hermes installer
- Workspace sections
- Extended sections: providers, voice, desktop, mail, messaging, doctor

This plan still creates all OS1 section routes so the app shape matches the reference from day one.

## File Structure

- Create `.gitignore`: ignore Node, build, Electron, logs, local env, and visual brainstorming artifacts.
- Create `package.json`: app metadata, dependencies, scripts, and electron-builder Windows target.
- Create `package-lock.json`: generated by `npm install`.
- Create `tsconfig.json`: shared TypeScript settings.
- Create `tsconfig.node.json`: Electron main/preload settings.
- Create `tsconfig.web.json`: React renderer settings.
- Create `vite.config.ts`: renderer Vite build.
- Create `electron.vite.config.ts`: Electron entry configuration for electron-vite.
- Create `vitest.config.ts`: unit/component test config.
- Create `index.html`: renderer entry.
- Create `src/main/main.ts`: BrowserWindow, security defaults, IPC registration.
- Create `src/main/ipc.ts`: typed IPC handlers.
- Create `src/main/appPaths.ts`: Windows-aware app paths.
- Create `src/preload/index.ts`: typed `window.os1` preload bridge.
- Create `src/shared/ipc.ts`: IPC channel names, types, and runtime helpers.
- Create `src/shared/sections.ts`: OS1 section metadata.
- Create `src/renderer/main.tsx`: React entry.
- Create `src/renderer/App.tsx`: top-level boot gate and shell.
- Create `src/renderer/styles/os1-tokens.css`: OS1 palette, typography, motion, layout tokens.
- Create `src/renderer/styles/global.css`: reset and app-level styling.
- Create `src/renderer/components/BootScreen.tsx`: OS1 boot screen.
- Create `src/renderer/components/BrandLockup.tsx`: OS1 brand mark.
- Create `src/renderer/components/Sidebar.tsx`: left navigation and voice row.
- Create `src/renderer/components/SectionFrame.tsx`: common page frame.
- Create `src/renderer/components/StatusPill.tsx`: status glass pill.
- Create `src/renderer/views/*.tsx`: initial route views for every OS1 section.
- Create `src/renderer/test/setup.ts`: Vitest DOM setup.
- Create `src/renderer/App.test.tsx`: shell smoke tests.
- Create `.github/workflows/ci.yml`: install, typecheck, test, build.
- Create `.github/workflows/release-windows.yml`: manual Windows release package.
- Create `README.md`: Windows install, development, and parity notes.

---

### Task 1: Project Metadata And Tooling

**Files:**
- Create: `.gitignore`
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `tsconfig.web.json`
- Create: `vite.config.ts`
- Create: `electron.vite.config.ts`
- Create: `vitest.config.ts`
- Create: `index.html`

- [ ] **Step 1: Create `.gitignore`**

```gitignore
node_modules/
dist/
dist-electron/
out/
release/
coverage/
.vite/
.turbo/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env
.env.*
!.env.example
.superpowers/
```

- [ ] **Step 2: Create `package.json`**

```json
{
  "name": "hermes-os1-windows",
  "version": "0.1.0",
  "description": "Windows desktop port of Hermes Desktop - OS1 Edition.",
  "author": "Abdulaziz",
  "license": "MIT",
  "private": false,
  "main": "dist-electron/main/main.js",
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "npm run typecheck && electron-vite build",
    "build:win": "npm run build && electron-builder --win nsis",
    "lint": "tsc --noEmit -p tsconfig.web.json && tsc --noEmit -p tsconfig.node.json",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "electron-updater": "^6.3.9",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@electron-toolkit/preload": "^3.0.1",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitest/coverage-v8": "^2.1.8",
    "electron": "^39.0.0",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^2.1.8"
  },
  "build": {
    "appId": "com.abdulazizbalu.hermesos1windows",
    "productName": "Hermes OS1 Windows",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "dist-electron/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "Hermes-OS1-Windows-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Hermes OS1 Windows"
    }
  }
}
```

- [ ] **Step 3: Create `tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "jsx": "react-jsx",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true
  }
}
```

- [ ] **Step 4: Create `tsconfig.node.json`**

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": [
      "node",
      "electron"
    ],
    "outDir": "dist-electron"
  },
  "include": [
    "src/main/**/*.ts",
    "src/preload/**/*.ts",
    "src/shared/**/*.ts",
    "electron.vite.config.ts"
  ]
}
```

- [ ] **Step 5: Create `tsconfig.web.json`**

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "lib": [
      "ES2022",
      "DOM",
      "DOM.Iterable"
    ],
    "types": [
      "vitest/globals",
      "@testing-library/jest-dom"
    ],
    "outDir": "dist"
  },
  "include": [
    "src/renderer/**/*.ts",
    "src/renderer/**/*.tsx",
    "src/shared/**/*.ts",
    "src/preload/**/*.d.ts",
    "vite.config.ts",
    "vitest.config.ts"
  ]
}
```

- [ ] **Step 6: Create `vite.config.ts`**

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  root: ".",
  build: {
    outDir: "dist",
    emptyOutDir: true
  },
  server: {
    port: 5173,
    strictPort: false
  }
});
```

- [ ] **Step 7: Create `electron.vite.config.ts`**

```ts
import { defineConfig, externalizeDepsPlugin } from "electron-vite";
import react from "@vitejs/plugin-react";
import { resolve } from "node:path";

export default defineConfig({
  main: {
    plugins: [externalizeDepsPlugin()],
    build: {
      rollupOptions: {
        input: {
          main: resolve(__dirname, "src/main/main.ts")
        }
      }
    }
  },
  preload: {
    plugins: [externalizeDepsPlugin()],
    build: {
      rollupOptions: {
        input: {
          index: resolve(__dirname, "src/preload/index.ts")
        }
      }
    }
  },
  renderer: {
    plugins: [react()],
    build: {
      rollupOptions: {
        input: {
          index: resolve(__dirname, "index.html")
        }
      }
    }
  }
});
```

- [ ] **Step 8: Create `vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["src/renderer/test/setup.ts"],
    coverage: {
      provider: "v8",
      reporter: ["text", "lcov"]
    }
  }
});
```

- [ ] **Step 9: Create `index.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hermes OS1 Windows</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/renderer/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 10: Install dependencies**

Run: `npm install`

Expected: npm creates `package-lock.json` and installs dependencies without audit-blocking errors.

- [ ] **Step 11: Run typecheck to verify empty project configuration**

Run: `npm run typecheck`

Expected: FAIL because source files do not exist yet. The failure should reference missing source entry points.

- [ ] **Step 12: Commit tooling**

```bash
git add .gitignore package.json package-lock.json tsconfig.json tsconfig.node.json tsconfig.web.json vite.config.ts electron.vite.config.ts vitest.config.ts index.html
git commit -m "chore: scaffold Electron React tooling"
```

---

### Task 2: Shared Types, IPC Skeleton, And Electron Main

**Files:**
- Create: `src/shared/sections.ts`
- Create: `src/shared/ipc.ts`
- Create: `src/main/appPaths.ts`
- Create: `src/main/ipc.ts`
- Create: `src/main/main.ts`
- Create: `src/preload/index.ts`
- Create: `src/preload/os1-api.d.ts`

- [ ] **Step 1: Create `src/shared/sections.ts`**

```ts
export const os1Sections = [
  { id: "connections", title: "Connections", icon: "Plug" },
  { id: "overview", title: "Overview", icon: "LayoutDashboard" },
  { id: "sessions", title: "Sessions", icon: "MessagesSquare" },
  { id: "cronjobs", title: "Cron Jobs", icon: "Clock3" },
  { id: "kanban", title: "Kanban", icon: "Columns3" },
  { id: "files", title: "Files", icon: "Files" },
  { id: "usage", title: "Usage", icon: "ChartNoAxesColumn" },
  { id: "skills", title: "Skills", icon: "Sparkles" },
  { id: "knowledgeBase", title: "Knowledge Base", icon: "BookOpen" },
  { id: "connectors", title: "Connectors", icon: "Cable" },
  { id: "providers", title: "Providers", icon: "KeyRound" },
  { id: "mail", title: "Mail", icon: "Mail" },
  { id: "messaging", title: "Messaging", icon: "MessageCircle" },
  { id: "terminal", title: "Terminal", icon: "Terminal" },
  { id: "doctor", title: "Doctor", icon: "Stethoscope" },
  { id: "desktop", title: "Desktop", icon: "Monitor" }
] as const;

export type OS1SectionId = (typeof os1Sections)[number]["id"];

export const defaultSectionId: OS1SectionId = "connections";
```

- [ ] **Step 2: Create `src/shared/ipc.ts`**

```ts
export const ipcChannels = {
  appInfo: "app:info",
  diagnostics: "app:diagnostics",
  connectionsList: "connections:list",
  connectionsSaveDraft: "connections:saveDraft"
} as const;

export interface AppInfo {
  name: string;
  version: string;
  platform: NodeJS.Platform;
  appDataPath: string;
}

export interface ConnectionDraft {
  label: string;
  transport: "orgo" | "ssh";
  destination: string;
}

export interface ConnectionSummary extends ConnectionDraft {
  id: string;
  createdAt: string;
}

export interface OS1Api {
  app: {
    info(): Promise<AppInfo>;
    diagnostics(): Promise<string>;
  };
  connections: {
    list(): Promise<ConnectionSummary[]>;
    saveDraft(draft: ConnectionDraft): Promise<ConnectionSummary>;
  };
}

export function assertConnectionDraft(value: unknown): ConnectionDraft {
  if (!value || typeof value !== "object") {
    throw new Error("Connection draft must be an object.");
  }

  const candidate = value as Record<string, unknown>;
  const label = String(candidate.label ?? "").trim();
  const destination = String(candidate.destination ?? "").trim();
  const transport = candidate.transport;

  if (!label) {
    throw new Error("Connection label is required.");
  }

  if (transport !== "orgo" && transport !== "ssh") {
    throw new Error("Connection transport must be orgo or ssh.");
  }

  if (!destination) {
    throw new Error("Connection destination is required.");
  }

  return { label, transport, destination };
}
```

- [ ] **Step 3: Create `src/main/appPaths.ts`**

```ts
import { app } from "electron";
import { join } from "node:path";

export interface OS1AppPaths {
  appData: string;
  logs: string;
  connections: string;
}

export function getOS1AppPaths(): OS1AppPaths {
  const appData = app.getPath("userData");

  return {
    appData,
    logs: join(appData, "logs"),
    connections: join(appData, "connections.json")
  };
}
```

- [ ] **Step 4: Create `src/main/ipc.ts`**

```ts
import { app, ipcMain } from "electron";
import { readFile, writeFile, mkdir } from "node:fs/promises";
import { dirname } from "node:path";
import {
  AppInfo,
  ConnectionSummary,
  assertConnectionDraft,
  ipcChannels
} from "../shared/ipc";
import { getOS1AppPaths } from "./appPaths";

async function readConnections(): Promise<ConnectionSummary[]> {
  const paths = getOS1AppPaths();

  try {
    const raw = await readFile(paths.connections, "utf8");
    const parsed = JSON.parse(raw) as ConnectionSummary[];
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
}

async function writeConnections(connections: ConnectionSummary[]): Promise<void> {
  const paths = getOS1AppPaths();
  await mkdir(dirname(paths.connections), { recursive: true });
  await writeFile(paths.connections, JSON.stringify(connections, null, 2), "utf8");
}

export function registerIpcHandlers(): void {
  ipcMain.handle(ipcChannels.appInfo, (): AppInfo => {
    const paths = getOS1AppPaths();

    return {
      name: app.getName(),
      version: app.getVersion(),
      platform: process.platform,
      appDataPath: paths.appData
    };
  });

  ipcMain.handle(ipcChannels.diagnostics, async (): Promise<string> => {
    const paths = getOS1AppPaths();
    const connections = await readConnections();

    return JSON.stringify(
      {
        app: app.getName(),
        version: app.getVersion(),
        platform: process.platform,
        appDataPath: paths.appData,
        connectionCount: connections.length
      },
      null,
      2
    );
  });

  ipcMain.handle(ipcChannels.connectionsList, async (): Promise<ConnectionSummary[]> => {
    return readConnections();
  });

  ipcMain.handle(ipcChannels.connectionsSaveDraft, async (_event, payload: unknown): Promise<ConnectionSummary> => {
    const draft = assertConnectionDraft(payload);
    const connections = await readConnections();
    const connection: ConnectionSummary = {
      ...draft,
      id: crypto.randomUUID(),
      createdAt: new Date().toISOString()
    };

    const next = [connection, ...connections];
    await writeConnections(next);
    return connection;
  });
}
```

- [ ] **Step 5: Create `src/main/main.ts`**

```ts
import { app, BrowserWindow, shell } from "electron";
import { join } from "node:path";
import { registerIpcHandlers } from "./ipc";

const isDevelopment = !app.isPackaged;

function createMainWindow(): BrowserWindow {
  const mainWindow = new BrowserWindow({
    width: 1280,
    height: 820,
    minWidth: 1040,
    minHeight: 680,
    show: false,
    title: "Hermes OS1 Windows",
    backgroundColor: "#C65A43",
    webPreferences: {
      preload: join(__dirname, "../preload/index.js"),
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: false
    }
  });

  mainWindow.on("ready-to-show", () => {
    mainWindow.show();
  });

  mainWindow.webContents.setWindowOpenHandler(({ url }) => {
    void shell.openExternal(url);
    return { action: "deny" };
  });

  if (isDevelopment && process.env.ELECTRON_RENDERER_URL) {
    void mainWindow.loadURL(process.env.ELECTRON_RENDERER_URL);
  } else {
    void mainWindow.loadFile(join(__dirname, "../../dist/index.html"));
  }

  return mainWindow;
}

app.whenReady().then(() => {
  registerIpcHandlers();
  createMainWindow();

  app.on("activate", () => {
    if (BrowserWindow.getAllWindows().length === 0) {
      createMainWindow();
    }
  });
});

app.on("window-all-closed", () => {
  if (process.platform !== "darwin") {
    app.quit();
  }
});
```

- [ ] **Step 6: Create `src/preload/index.ts`**

```ts
import { contextBridge, ipcRenderer } from "electron";
import { ConnectionDraft, OS1Api, ipcChannels } from "../shared/ipc";

const api: OS1Api = {
  app: {
    info: () => ipcRenderer.invoke(ipcChannels.appInfo),
    diagnostics: () => ipcRenderer.invoke(ipcChannels.diagnostics)
  },
  connections: {
    list: () => ipcRenderer.invoke(ipcChannels.connectionsList),
    saveDraft: (draft: ConnectionDraft) => ipcRenderer.invoke(ipcChannels.connectionsSaveDraft, draft)
  }
};

contextBridge.exposeInMainWorld("os1", api);
```

- [ ] **Step 7: Create `src/preload/os1-api.d.ts`**

```ts
import { OS1Api } from "../shared/ipc";

declare global {
  interface Window {
    os1: OS1Api;
  }
}

export {};
```

- [ ] **Step 8: Run typecheck**

Run: `npm run typecheck`

Expected: FAIL because renderer files are still missing.

- [ ] **Step 9: Commit Electron main and IPC**

```bash
git add src/shared src/main src/preload
git commit -m "feat: add secure Electron IPC foundation"
```

---

### Task 3: Renderer Entry, OS1 Tokens, And App Shell

**Files:**
- Create: `src/renderer/main.tsx`
- Create: `src/renderer/App.tsx`
- Create: `src/renderer/styles/os1-tokens.css`
- Create: `src/renderer/styles/global.css`
- Create: `src/renderer/components/BrandLockup.tsx`
- Create: `src/renderer/components/Sidebar.tsx`
- Create: `src/renderer/components/SectionFrame.tsx`
- Create: `src/renderer/components/StatusPill.tsx`

- [ ] **Step 1: Create `src/renderer/styles/os1-tokens.css`**

```css
:root {
  --os1-bg-cream: #d9c9ad;
  --os1-bg-beige: #cfc0a6;
  --os1-coral: #c65a43;
  --os1-coral-500: #c1553d;
  --os1-coral-400: #d87660;
  --os1-coral-300: #e6ad86;
  --os1-coral-600: #a84832;
  --os1-on-coral-primary: rgba(255, 255, 255, 0.95);
  --os1-on-coral-secondary: rgba(255, 255, 255, 0.7);
  --os1-on-coral-muted: rgba(255, 255, 255, 0.5);
  --os1-on-cream-primary: rgba(56, 40, 30, 0.92);
  --os1-on-cream-secondary: rgba(72, 56, 44, 0.72);
  --os1-on-cream-muted: rgba(88, 72, 60, 0.5);
  --os1-glass-fill: rgba(255, 255, 255, 0.1);
  --os1-glass-border: rgba(255, 255, 255, 0.3);
  --os1-glass-fill-hover: rgba(255, 255, 255, 0.2);
  --os1-glass-border-hover: rgba(255, 255, 255, 0.5);
  --os1-dark-overlay: rgba(40, 30, 24, 0.18);
  --os1-warning: #e2a042;
  --os1-danger: #c65a43;
  --os1-success: #7ba88e;
  --os1-font-sans: "DM Sans", "Segoe UI", system-ui, sans-serif;
  --os1-font-mono: "Cascadia Code", "SFMono-Regular", Consolas, monospace;
  --os1-motion-fast: 300ms;
  --os1-motion-medium: 600ms;
  --os1-motion-slow: 1200ms;
}
```

- [ ] **Step 2: Create `src/renderer/styles/global.css`**

```css
@import "./os1-tokens.css";

* {
  box-sizing: border-box;
}

html,
body,
#root {
  width: 100%;
  height: 100%;
}

body {
  margin: 0;
  background: var(--os1-coral);
  color: var(--os1-on-cream-primary);
  font-family: var(--os1-font-sans);
  letter-spacing: 0;
  overflow: hidden;
}

button,
input,
textarea,
select {
  font: inherit;
}

button {
  border: 0;
}

.os1-app {
  display: grid;
  grid-template-columns: minmax(196px, 220px) minmax(0, 1fr);
  width: 100%;
  height: 100%;
  background: var(--os1-coral);
}

.os1-detail {
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  background: var(--os1-bg-cream);
}

.os1-scroll {
  overflow: auto;
  min-height: 0;
}

.os1-glass {
  background: var(--os1-glass-fill);
  border: 1px solid var(--os1-glass-border);
  backdrop-filter: blur(18px);
}

.os1-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-height: 34px;
  padding: 0 13px;
  color: var(--os1-on-coral-primary);
  background: var(--os1-glass-fill);
  border: 1px solid var(--os1-glass-border);
  border-radius: 8px;
  cursor: pointer;
  transition:
    background var(--os1-motion-fast) ease,
    border-color var(--os1-motion-fast) ease,
    transform var(--os1-motion-fast) ease;
}

.os1-button:hover {
  background: var(--os1-glass-fill-hover);
  border-color: var(--os1-glass-border-hover);
}

.os1-button:active {
  transform: translateY(1px);
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 1ms !important;
    transition-duration: 1ms !important;
  }
}
```

- [ ] **Step 3: Create `src/renderer/main.tsx`**

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import "./styles/global.css";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

- [ ] **Step 4: Create `src/renderer/components/BrandLockup.tsx`**

```tsx
export function BrandLockup(): JSX.Element {
  return (
    <div className="brand-lockup" aria-label="OS1 by Element Software">
      <div className="brand-lockup__name">
        <span>Element</span>
        <span>Software</span>
      </div>
      <div className="brand-lockup__descriptor">OS1 · COMPUTER USE</div>
    </div>
  );
}
```

- [ ] **Step 5: Create `src/renderer/components/StatusPill.tsx`**

```tsx
interface StatusPillProps {
  tone: "muted" | "warning" | "success" | "danger";
  children: string;
}

export function StatusPill({ tone, children }: StatusPillProps): JSX.Element {
  return <span className={`status-pill status-pill--${tone}`}>{children}</span>;
}
```

- [ ] **Step 6: Create `src/renderer/components/SectionFrame.tsx`**

```tsx
import { ReactNode } from "react";

interface SectionFrameProps {
  eyebrow: string;
  title: string;
  description: string;
  children?: ReactNode;
}

export function SectionFrame({ eyebrow, title, description, children }: SectionFrameProps): JSX.Element {
  return (
    <main className="section-frame">
      <header className="section-frame__header">
        <div>
          <p className="section-frame__eyebrow">{eyebrow}</p>
          <h1>{title}</h1>
        </div>
        <p>{description}</p>
      </header>
      <div className="section-frame__body">{children}</div>
    </main>
  );
}
```

- [ ] **Step 7: Create `src/renderer/components/Sidebar.tsx`**

```tsx
import {
  BookOpen,
  Cable,
  ChartNoAxesColumn,
  Clock3,
  Columns3,
  Files,
  KeyRound,
  LayoutDashboard,
  Mail,
  MessageCircle,
  MessagesSquare,
  MicOff,
  Monitor,
  Plug,
  Sparkles,
  Stethoscope,
  Terminal
} from "lucide-react";
import { os1Sections, OS1SectionId } from "../../shared/sections";
import { BrandLockup } from "./BrandLockup";
import { StatusPill } from "./StatusPill";

const icons = {
  BookOpen,
  Cable,
  ChartNoAxesColumn,
  Clock3,
  Columns3,
  Files,
  KeyRound,
  LayoutDashboard,
  Mail,
  MessageCircle,
  MessagesSquare,
  Monitor,
  Plug,
  Sparkles,
  Stethoscope,
  Terminal
};

interface SidebarProps {
  activeSection: OS1SectionId;
  onSelectSection(section: OS1SectionId): void;
}

export function Sidebar({ activeSection, onSelectSection }: SidebarProps): JSX.Element {
  return (
    <aside className="sidebar">
      <div className="sidebar__scroll os1-scroll">
        <BrandLockup />
        <section className="sidebar-card">
          <h2>No active host</h2>
          <p>Connect an Orgo VM or SSH host.</p>
          <StatusPill tone="muted">OFFLINE</StatusPill>
        </section>
        <nav className="sidebar__nav" aria-label="OS1 sections">
          {os1Sections.map((section) => {
            const Icon = icons[section.icon];
            const selected = activeSection === section.id;

            return (
              <button
                key={section.id}
                className={`sidebar__item ${selected ? "sidebar__item--selected" : ""}`}
                type="button"
                onClick={() => onSelectSection(section.id)}
              >
                <Icon size={16} strokeWidth={1.8} aria-hidden="true" />
                <span>{section.title}</span>
              </button>
            );
          })}
        </nav>
      </div>
      <button className="voice-row" type="button">
        <MicOff size={16} aria-hidden="true" />
        <span>Voice</span>
        <strong>OFF</strong>
      </button>
    </aside>
  );
}
```

- [ ] **Step 8: Create `src/renderer/App.tsx`**

```tsx
import { useMemo, useState } from "react";
import { defaultSectionId, OS1SectionId } from "../shared/sections";
import { Sidebar } from "./components/Sidebar";
import { BootScreen } from "./components/BootScreen";
import { getSectionView } from "./views";

export default function App(): JSX.Element {
  const [bootComplete, setBootComplete] = useState(false);
  const [activeSection, setActiveSection] = useState<OS1SectionId>(defaultSectionId);
  const ActiveView = useMemo(() => getSectionView(activeSection), [activeSection]);

  if (!bootComplete) {
    return <BootScreen onComplete={() => setBootComplete(true)} />;
  }

  return (
    <div className="os1-app">
      <Sidebar activeSection={activeSection} onSelectSection={setActiveSection} />
      <section className="os1-detail">
        <ActiveView />
      </section>
    </div>
  );
}
```

- [ ] **Step 9: Add shell CSS to `src/renderer/styles/global.css`**

Append:

```css
.brand-lockup {
  padding: 18px 16px 8px;
  color: var(--os1-on-coral-primary);
}

.brand-lockup__name {
  display: flex;
  gap: 4px;
  font-family: Arial, Helvetica, sans-serif;
  font-size: 18px;
  font-style: italic;
  font-weight: 800;
  line-height: 1;
}

.brand-lockup__name span:last-child {
  font-weight: 400;
}

.brand-lockup__descriptor {
  margin-top: 6px;
  font-size: 8px;
  font-weight: 700;
  letter-spacing: 1.4px;
  text-transform: uppercase;
  color: var(--os1-on-coral-secondary);
}

.sidebar {
  display: flex;
  min-width: 0;
  min-height: 0;
  flex-direction: column;
  color: var(--os1-on-coral-secondary);
  background: var(--os1-coral);
}

.sidebar__scroll {
  flex: 1;
  padding-bottom: 18px;
}

.sidebar-card {
  margin: 10px;
  padding: 10px;
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.08);
}

.sidebar-card h2 {
  margin: 0 0 4px;
  color: var(--os1-on-coral-primary);
  font-size: 17px;
  font-weight: 300;
}

.sidebar-card p {
  margin: 0 0 10px;
  font-size: 12px;
  color: var(--os1-on-coral-muted);
}

.sidebar__nav {
  display: grid;
  gap: 2px;
  padding: 0 10px;
}

.sidebar__item,
.voice-row {
  display: grid;
  grid-template-columns: 18px minmax(0, 1fr) auto;
  align-items: center;
  gap: 10px;
  width: 100%;
  border-radius: 8px;
  color: inherit;
  background: transparent;
  cursor: pointer;
  text-align: left;
}

.sidebar__item {
  padding: 7px 12px;
}

.sidebar__item span,
.voice-row span {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 14px;
  font-weight: 300;
}

.sidebar__item--selected {
  color: var(--os1-on-coral-primary);
  background: var(--os1-glass-fill);
  box-shadow: inset 0 0 0 1px var(--os1-glass-border);
}

.sidebar__item:hover,
.voice-row:hover {
  color: var(--os1-on-coral-primary);
  background: var(--os1-glass-fill-hover);
}

.voice-row {
  margin: 0 10px 14px;
  padding: 9px 12px;
  color: var(--os1-on-coral-secondary);
}

.voice-row strong {
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 1.6px;
}

.status-pill {
  display: inline-flex;
  align-items: center;
  min-height: 22px;
  padding: 0 8px;
  border-radius: 999px;
  font-size: 10px;
  letter-spacing: 1.4px;
  text-transform: uppercase;
}

.status-pill--muted {
  color: var(--os1-on-coral-secondary);
  background: var(--os1-dark-overlay);
}

.status-pill--warning {
  color: #38281e;
  background: var(--os1-warning);
}

.status-pill--success {
  color: #102219;
  background: var(--os1-success);
}

.status-pill--danger {
  color: white;
  background: var(--os1-danger);
}

.section-frame {
  min-width: 0;
  height: 100%;
  padding: 28px;
  overflow: auto;
}

.section-frame__header {
  display: grid;
  grid-template-columns: minmax(260px, 0.7fr) minmax(320px, 1.3fr);
  gap: 18px;
  align-items: stretch;
}

.section-frame__header > * {
  padding: 22px;
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.22);
  box-shadow: inset 0 0 0 1px rgba(56, 40, 30, 0.12);
}

.section-frame__eyebrow {
  margin: 0 0 12px;
  color: var(--os1-on-cream-muted);
  font-size: 11px;
  letter-spacing: 1.6px;
  text-transform: uppercase;
}

.section-frame h1 {
  margin: 0;
  color: var(--os1-on-cream-primary);
  font-size: 34px;
  font-weight: 300;
  letter-spacing: 1.5px;
}

.section-frame__header p {
  margin: 0;
  color: var(--os1-on-cream-secondary);
  font-size: 15px;
  font-weight: 300;
  line-height: 1.7;
}

.section-frame__body {
  margin-top: 18px;
}
```

- [ ] **Step 10: Run typecheck**

Run: `npm run typecheck`

Expected: FAIL because `BootScreen` and route views are still missing.

- [ ] **Step 11: Commit renderer shell**

```bash
git add src/renderer/main.tsx src/renderer/App.tsx src/renderer/styles src/renderer/components
git commit -m "feat: add OS1 renderer shell"
```

---

### Task 4: Boot Screen And Section Views

**Files:**
- Create: `src/renderer/components/BootScreen.tsx`
- Create: `src/renderer/views/index.ts`
- Create: `src/renderer/views/ConnectionsView.tsx`
- Create: `src/renderer/views/SectionStubView.tsx`

- [ ] **Step 1: Create `src/renderer/components/BootScreen.tsx`**

```tsx
import { useEffect, useState } from "react";
import { BrandLockup } from "./BrandLockup";

interface BootScreenProps {
  onComplete(): void;
}

export function BootScreen({ onComplete }: BootScreenProps): JSX.Element {
  const [ready, setReady] = useState(false);

  useEffect(() => {
    const timer = window.setTimeout(() => setReady(true), 1200);
    return () => window.clearTimeout(timer);
  }, []);

  return (
    <main className="boot-screen">
      <BrandLockup />
      <div className="boot-screen__center">
        <div className="boot-screen__ring" aria-hidden="true" />
        <h1>OS1</h1>
        <p>Hermes workspace for Windows</p>
        <button className="os1-button" type="button" disabled={!ready} onClick={onComplete}>
          {ready ? "Begin" : "Starting"}
        </button>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Create `src/renderer/views/SectionStubView.tsx`**

```tsx
import { SectionFrame } from "../components/SectionFrame";

interface SectionStubViewProps {
  eyebrow: string;
  title: string;
  description: string;
}

export function SectionStubView({ eyebrow, title, description }: SectionStubViewProps): JSX.Element {
  return (
    <SectionFrame eyebrow={eyebrow} title={title} description={description}>
      <div className="empty-panel">
        <p>Windows parity surface is reserved for this OS1 section.</p>
      </div>
    </SectionFrame>
  );
}
```

- [ ] **Step 3: Create `src/renderer/views/ConnectionsView.tsx`**

```tsx
import { FormEvent, useEffect, useState } from "react";
import { ConnectionSummary } from "../../shared/ipc";
import { SectionFrame } from "../components/SectionFrame";
import { StatusPill } from "../components/StatusPill";

export function ConnectionsView(): JSX.Element {
  const [connections, setConnections] = useState<ConnectionSummary[]>([]);
  const [label, setLabel] = useState("Orgo VM");
  const [destination, setDestination] = useState("orgo workspace");
  const [status, setStatus] = useState("Ready for Orgo parity implementation.");

  useEffect(() => {
    void window.os1.connections.list().then(setConnections);
  }, []);

  async function saveDraft(event: FormEvent<HTMLFormElement>): Promise<void> {
    event.preventDefault();
    const saved = await window.os1.connections.saveDraft({
      label,
      destination,
      transport: "orgo"
    });
    setConnections((current) => [saved, ...current]);
    setStatus(`Saved ${saved.label}.`);
  }

  return (
    <SectionFrame
      eyebrow="Connections"
      title="Connect the workspace"
      description="The first parity milestone wires the OS1 shell and safe IPC. The next milestone turns this draft surface into real Orgo key verification, workspace selection, and computer creation."
    >
      <div className="connections-grid">
        <form className="panel-form" onSubmit={saveDraft}>
          <label>
            <span>Label</span>
            <input value={label} onChange={(event) => setLabel(event.target.value)} />
          </label>
          <label>
            <span>Destination</span>
            <input value={destination} onChange={(event) => setDestination(event.target.value)} />
          </label>
          <button className="os1-button" type="submit">Save Draft</button>
          <p>{status}</p>
        </form>
        <div className="connection-list">
          <StatusPill tone={connections.length > 0 ? "success" : "muted"}>
            {connections.length > 0 ? "DRAFTS SAVED" : "NO HOSTS"}
          </StatusPill>
          {connections.map((connection) => (
            <article key={connection.id} className="connection-card">
              <h2>{connection.label}</h2>
              <p>{connection.destination}</p>
              <span>{connection.transport}</span>
            </article>
          ))}
        </div>
      </div>
    </SectionFrame>
  );
}
```

- [ ] **Step 4: Create `src/renderer/views/index.ts`**

```ts
import { ComponentType } from "react";
import { OS1SectionId } from "../../shared/sections";
import { ConnectionsView } from "./ConnectionsView";
import { SectionStubView } from "./SectionStubView";

export function getSectionView(section: OS1SectionId): ComponentType {
  if (section === "connections") {
    return ConnectionsView;
  }

  const titles: Record<OS1SectionId, string> = {
    connections: "Connections",
    overview: "Overview",
    sessions: "Sessions",
    cronjobs: "Cron Jobs",
    kanban: "Kanban",
    files: "Files",
    usage: "Usage",
    skills: "Skills",
    knowledgeBase: "Knowledge Base",
    connectors: "Connectors",
    providers: "Providers",
    mail: "Mail",
    messaging: "Messaging",
    terminal: "Terminal",
    doctor: "Doctor",
    desktop: "Desktop"
  };

  return function View(): JSX.Element {
    return (
      <SectionStubView
        eyebrow={titles[section]}
        title={titles[section]}
        description={`${titles[section]} will keep the same OS1 host-first workflow when its parity slice is implemented.`}
      />
    );
  };
}
```

- [ ] **Step 5: Add boot and view CSS to `src/renderer/styles/global.css`**

Append:

```css
.boot-screen {
  display: grid;
  grid-template-rows: auto 1fr;
  width: 100%;
  height: 100%;
  color: var(--os1-on-coral-primary);
  background:
    radial-gradient(circle at center, rgba(255, 255, 255, 0.16), transparent 34%),
    var(--os1-coral);
}

.boot-screen__center {
  display: grid;
  place-items: center;
  align-content: center;
  gap: 14px;
  min-height: 0;
}

.boot-screen__ring {
  width: 148px;
  height: 148px;
  border: 1px solid var(--os1-glass-border);
  border-top-color: var(--os1-on-coral-primary);
  border-radius: 50%;
  animation: os1-spin var(--os1-motion-slow) linear infinite;
}

.boot-screen h1 {
  margin: 0;
  font-size: 96px;
  font-weight: 200;
  letter-spacing: 8px;
}

.boot-screen p {
  margin: 0 0 12px;
  color: var(--os1-on-coral-secondary);
  font-size: 14px;
  font-weight: 300;
  letter-spacing: 0.4px;
}

@keyframes os1-spin {
  to {
    transform: rotate(360deg);
  }
}

.empty-panel,
.panel-form,
.connection-list {
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.22);
  box-shadow: inset 0 0 0 1px rgba(56, 40, 30, 0.12);
}

.empty-panel {
  padding: 22px;
  color: var(--os1-on-cream-secondary);
}

.connections-grid {
  display: grid;
  grid-template-columns: minmax(320px, 420px) minmax(0, 1fr);
  gap: 18px;
}

.panel-form {
  display: grid;
  gap: 14px;
  padding: 18px;
}

.panel-form label {
  display: grid;
  gap: 6px;
}

.panel-form label span {
  color: var(--os1-on-cream-muted);
  font-size: 11px;
  letter-spacing: 1.4px;
  text-transform: uppercase;
}

.panel-form input {
  min-height: 36px;
  padding: 0 11px;
  border: 1px solid rgba(56, 40, 30, 0.18);
  border-radius: 8px;
  color: var(--os1-on-cream-primary);
  background: rgba(255, 255, 255, 0.26);
  outline: none;
}

.panel-form .os1-button {
  justify-self: start;
  color: var(--os1-on-coral-primary);
  background: var(--os1-coral);
}

.panel-form p {
  margin: 0;
  color: var(--os1-on-cream-secondary);
  font-size: 13px;
}

.connection-list {
  display: grid;
  align-content: start;
  gap: 12px;
  padding: 18px;
}

.connection-card {
  padding: 14px;
  border-radius: 8px;
  background: rgba(255, 255, 255, 0.18);
}

.connection-card h2 {
  margin: 0 0 4px;
  font-size: 17px;
  font-weight: 300;
}

.connection-card p {
  margin: 0 0 8px;
  color: var(--os1-on-cream-secondary);
}

.connection-card span {
  color: var(--os1-on-cream-muted);
  font-size: 11px;
  letter-spacing: 1.4px;
  text-transform: uppercase;
}
```

- [ ] **Step 6: Run typecheck**

Run: `npm run typecheck`

Expected: PASS.

- [ ] **Step 7: Run dev server smoke check**

Run: `npm run dev`

Expected: Electron launches, shows OS1 boot screen, Begin button appears after roughly 1.2 seconds, sidebar appears after clicking Begin, and every sidebar section opens without crashing.

- [ ] **Step 8: Commit boot and views**

```bash
git add src/renderer/components/BootScreen.tsx src/renderer/views src/renderer/styles/global.css
git commit -m "feat: add OS1 boot and section surfaces"
```

---

### Task 5: Tests For Shell And IPC Payloads

**Files:**
- Create: `src/renderer/test/setup.ts`
- Create: `src/renderer/App.test.tsx`
- Create: `src/shared/ipc.test.ts`

- [ ] **Step 1: Create `src/renderer/test/setup.ts`**

```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 2: Create `src/renderer/App.test.tsx`**

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { beforeEach, describe, expect, it, vi } from "vitest";
import App from "./App";

beforeEach(() => {
  window.os1 = {
    app: {
      info: vi.fn(),
      diagnostics: vi.fn()
    },
    connections: {
      list: vi.fn().mockResolvedValue([]),
      saveDraft: vi.fn().mockImplementation(async (draft) => ({
        ...draft,
        id: "connection-1",
        createdAt: "2026-05-12T00:00:00.000Z"
      }))
    }
  };
});

describe("OS1 shell", () => {
  it("shows boot screen before entering the workspace", async () => {
    render(<App />);

    expect(screen.getByText("OS1")).toBeInTheDocument();
    await waitFor(() => expect(screen.getByRole("button", { name: "Begin" })).toBeEnabled());
  });

  it("enters workspace and opens section routes", async () => {
    const user = userEvent.setup();
    render(<App />);

    await waitFor(() => expect(screen.getByRole("button", { name: "Begin" })).toBeEnabled());
    await user.click(screen.getByRole("button", { name: "Begin" }));
    await user.click(screen.getByRole("button", { name: "Terminal" }));

    expect(screen.getByRole("heading", { name: "Terminal" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Create `src/shared/ipc.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { assertConnectionDraft } from "./ipc";

describe("assertConnectionDraft", () => {
  it("normalizes valid connection drafts", () => {
    expect(
      assertConnectionDraft({
        label: " Orgo VM ",
        transport: "orgo",
        destination: " workspace "
      })
    ).toEqual({
      label: "Orgo VM",
      transport: "orgo",
      destination: "workspace"
    });
  });

  it("rejects missing labels", () => {
    expect(() =>
      assertConnectionDraft({
        label: "",
        transport: "orgo",
        destination: "workspace"
      })
    ).toThrow("Connection label is required.");
  });
});
```

- [ ] **Step 4: Install `@testing-library/user-event`**

Run: `npm install -D @testing-library/user-event`

Expected: `package.json` and `package-lock.json` include `@testing-library/user-event`.

- [ ] **Step 5: Run tests**

Run: `npm run test`

Expected: PASS with `OS1 shell` and `assertConnectionDraft` tests passing.

- [ ] **Step 6: Commit tests**

```bash
git add package.json package-lock.json src/renderer/test src/renderer/App.test.tsx src/shared/ipc.test.ts
git commit -m "test: cover OS1 shell and IPC validation"
```

---

### Task 6: CI, Windows Release Workflow, And README

**Files:**
- Create: `.github/workflows/ci.yml`
- Create: `.github/workflows/release-windows.yml`
- Create: `README.md`

- [ ] **Step 1: Create `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
```

- [ ] **Step 2: Create `.github/workflows/release-windows.yml`**

```yaml
name: Release Windows

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version, for example 0.1.0
        required: true

jobs:
  package-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Windows installer
        run: npm run build:win

      - name: Create checksums
        shell: pwsh
        run: |
          Get-ChildItem release -File | ForEach-Object {
            Get-FileHash $_.FullName -Algorithm SHA256 |
              ForEach-Object { "$($_.Hash)  $([System.IO.Path]::GetFileName($_.Path))" } |
              Out-File -Encoding ascii -FilePath "$($_.FullName).sha256"
          }

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}
          name: Hermes OS1 Windows v${{ inputs.version }}
          draft: true
          files: |
            release/*
```

- [ ] **Step 3: Create `README.md`**

```md
# Hermes OS1 Windows

Windows desktop port of Hermes Desktop - OS1 Edition.

This project preserves the OS1 product shape, visual design, and host-first workflows while replacing macOS-only implementation details with Windows-compatible equivalents.

## Current Status

The first slice provides:

- Electron + React + TypeScript desktop shell
- OS1 coral/cream design tokens
- OS1 boot screen
- Sidebar and all OS1 section routes
- Typed secure IPC skeleton
- Windows installer workflow

Upcoming slices add:

- Orgo API key verification and secure credential storage
- Workspace and computer picker
- Hermes Agent install on Orgo VM
- WebSocket terminal
- Overview, Sessions, Kanban, Files, Skills, Cron, Usage
- Providers, Voice, Desktop/VNC, Mail, Messaging, Doctor

## Development

```sh
npm install
npm run dev
```

## Checks

```sh
npm run typecheck
npm run test
npm run build
```

## Build Windows Installer

```sh
npm run build:win
```

The installer is written to `release/`.

## Windows SmartScreen

Early builds are unsigned. Windows SmartScreen may show a warning on first launch. Use only installers from this repository's GitHub Releases.

## Design Reference

Reference product: `nickvasilescu/hermes-desktop-os1`.

Parity spec: `docs/superpowers/specs/2026-05-12-os1-windows-parity-design.md`.
```

- [ ] **Step 4: Run all checks**

Run: `npm run typecheck`

Expected: PASS.

Run: `npm run test`

Expected: PASS.

Run: `npm run build`

Expected: PASS and creates `dist` plus `dist-electron`.

- [ ] **Step 5: Build Windows installer locally when dependencies are available**

Run: `npm run build:win`

Expected: PASS and creates `release/Hermes-OS1-Windows-0.1.0-x64.exe`.

- [ ] **Step 6: Commit CI and README**

```bash
git add .github README.md
git commit -m "ci: add Windows build and release workflows"
```

---

### Task 7: Final Verification And Push

**Files:**
- Modify only if checks reveal concrete issues.

- [ ] **Step 1: Run final status**

Run: `git status --short`

Expected: clean working tree.

- [ ] **Step 2: Run final verification**

Run: `npm run typecheck`

Expected: PASS.

Run: `npm run test`

Expected: PASS.

Run: `npm run build`

Expected: PASS.

- [ ] **Step 3: Push to GitHub**

```bash
git push origin main
```

Expected: GitHub repository `abdulazizbalu/hermes-os1-windows` contains the Electron OS1 shell and CI workflows.

## Self-Review

Spec coverage:

- Covers shell, boot, navigation, design tokens, typed IPC, release workflow, and tests from the approved spec.
- Defers Orgo credential verification, VM picker, Hermes installer, terminal, and workspace sections to later implementation plans because those are separate subsystems.

Placeholder scan:

- No placeholder markers, incomplete file names, or undefined task boundaries.

Type consistency:

- `OS1SectionId`, `OS1Api`, `ConnectionDraft`, and `ConnectionSummary` are defined before use.
- IPC channel names are centralized in `src/shared/ipc.ts`.
- Renderer imports shared types through stable relative paths.
